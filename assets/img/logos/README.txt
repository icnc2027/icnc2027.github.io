Drop partner/sponsor logos here with EXACTLY these file names (PNG or SVG, transparent background, ~600 px wide):
  ucalgary.png   schulich.png   ci.png   sandia.png   yale.png   cics.png
Missing images are hidden automatically (assets/js/main.js), so pages never show a broken image.
To add a sponsor logo, add the file here and one <img data-optional ...> line in the relevant page.
